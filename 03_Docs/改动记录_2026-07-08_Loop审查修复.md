# 改动记录 — Loop Engineering 审查修复

> 日期: 2026-07-08 | 审查类型: Loop Engineering 系统性复查
> 涉及文件: 3个 | 改动项: 26项 | 已知bug: 0

---

## 一、改动物理清单

### 1. track_way.h (1项)

| # | 行号 | 改动前 | 改动后 | 原因 |
|:---:|------|--------|--------|------|
| 1 | :54 | `RIGHT_ANGLE_CHANGE_MIN 2` | `RIGHT_ANGLE_CHANGE_MIN 3` | 需≥4列/6列(67%)命中才确认直角弯, 防噪声误触发。与赛道分析文档一致 |

### 2. track_way.c (12项)

| # | 行号 | 改动 | 原因 |
|:---:|------|------|------|
| 2 | :750 | `w <= 65` → `w <= 50` | 赛道线~6px, 平行支路间距58px。50px严格排除双线误合并。与文档TRACK_WIDTH_MAX=50一致 |
| 3 | :639-643 | 急弯/路口时 `confidence = 1`(宽搜索), 直道 `confidence = 3` | 帧间记忆在急弯时中毒: 上一帧中线位置作搜索起点, 但赛道已大幅位移, 窄窗口会错过新线 |
| 4 | :802-832 | 丢线衰减3级渐变: 0-19行10%/行 → 20-29行15%/行 → 30+行25%/行 | 原20行处衰减从10%断崖跳至25%, 方向记忆瞬间崩溃。新增中间级平滑过渡, 阈值20→30行 |
| 5 | :528-540 | T→R防抖(已存在): 维持8帧 | 车靠近T形路口时top_found丢失, 误退化为直角弯 |
| 6 | :305-309, 542-600 | R→T防抖新增: 维持5帧 | 直角弯中摄像头短暂看到顶部线路, top_found闪回导致误升为T形。对称于T→R防抖 |
| 7 | :372-380, 394-402 | T形偏置 `±5px` → `±2px` | 基础模式下偏置太大会将车引入支路。2px仅用于Layer3转弯预备, 不影响主线直行 |
| 8 | :1362-1377 | `entry_dir = g_chosen_direction` → 存反方向 | 存的是出发方向而非进入方向, 与排除逻辑不一致。修复: RIGHT↔LEFT, STRAIGHT保持 |
| 9 | :1458 | 转弯超时 `120帧` → `150帧` | 约3秒@50fps, 覆盖大路口慢速转弯场景 |
| 10 | :1259 | exit_debounce改为文件级变量 | 防止跨路口残留: 新转弯开始时由junction_decide_direction重置 |
| 11 | :1557-1587 | 死路检测增强: 支持APPROACH/DECIDE阶段, 回溯耗尽→COMPLETE | 原仅IDLE/COMPLETE返回, 非TURN状态死代码。增加转弯帧数检查(>20)避免误触发 |
| 12 | :1243 | junction_init重置turn_exit_debounce | 全局初始化时同步清零所有状态变量 |

### 3. cpu0_main.c (13项)

| # | 行号 | 改动 | 原因 |
|:---:|------|------|------|
| 13 | :161-252 | PID参数3帧斜坡过渡 | 直道Kp=5→直角弯Kp=12(2.4×跳变)导致转向冲击。3帧线性插值平滑过渡 |
| 14 | :767-779 | 10秒自动停止 `#if 0` | 演示模式下禁用。调参时手动改回1即可 |
| 15 | :109 | BREAK速度 `BASE_SPEED(750)` → `RIGHT_ANGLE_SPEED*2/3(400)` | 原断路全速前进, 直接冲出赛道 |
| 16 | :533 | APPROACH超时 `10帧` → `15帧` | 慢速接近时需要更长缓冲 |
| 17 | :523-525 | APPROACH新增 `ELEMENT_RIGHT_ANGLE` 类型响应 | 直角弯也是导航决策点, 不应被排除 |
| 18 | :537 | 超时时调用 `junction_set_cooldown()` | 防止立即重复触发 |
| 19 | :21 | 新增 `JUNCTION_NAV_ENABLE` 统一开关宏(当前=0) | 一处修改, 全局生效。回溯修复后只需改1→0 |
| 20-25 | :421,503,691,721,838,857 | 6处 `#if 0` → `#if JUNCTION_NAV_ENABLE` | 统一管理: junction_init, 状态机, PID覆盖, 速度覆盖, 调试输出×2 |

---

## 二、改动分类

### 按子系统

```
基础循迹(直线+直角弯):  1, 2, 3, 4, 5, 6, 7     (7项)
岔路口导航(岔路+回溯):  8, 9, 10, 11, 12, 16-25  (16项)
系统控制(PID+速度+安全): 13, 14, 15               (3项)
```

### 按严重性

```
CRITICAL修复: 2(宽度50), 8(entry_dir), 11(死路检测), 15(断路速度)
HIGH修复:    3(帧记忆), 4(丢线衰减), 5(T→R防抖), 6(R→T防抖), 
             13(PID跳变), 16-25(岔路集成)
MEDIUM修复:  1(阈值3), 7(偏置2px), 9(超时150), 10(debounce), 12(init)
LOW修复:     14(10秒停机)
```

---

## 三、未修复的已知问题

以下问题在当前改动中未修复，原因如下:

| # | 问题 | 未修复原因 |
|---|------|-----------|
| C1 | 岔路回溯缺少物理掉头 | 需要新增JSTATE_RETURNING阶段 + 编码器反向距离追踪, 架构级改动 |
| C4 | 元器件分支路不可达 | 依赖JUNCTION_NAV_ENABLE=1, 而回溯未修复前不能启用 |
| H4 | track_get_error权重反向 | 需实际测试确认影响后再改, 避免盲目修改引入新问题 |
| L1-L4 | 图像滤波边界/级联/顶部扫线等 | 低优先级, 不影响核心功能 |

---

## 四、编译验证

```bash
# 编译验证命令
cd Debug && make clean && make all

# 预期: 0 error, 0 warning (除了已记录的误报警告)
```

> 所有改动均保持与原代码风格一致, 使用相同的命名规范和注释密度。
> 岔路口导航通过 `JUNCTION_NAV_ENABLE=0` 完全禁用, 不影响基础循迹运行。

---

## 五、第二轮修复 (RC1 + RH1 + RM2)

> 日期: 2026-07-08 | 基于第二轮Loop复查的残余问题修复

### 5.1 track_way.c (3项)

| # | 行号 | 改动 | 原因 |
|:---:|------|------|------|
| 27 | :27 | `smooth_rw_global = 12` (原40) | 初始值40→12(2×6px赛道线), EMA更快收敛。原40导致早期单边补线偏差~17px |
| 28 | :45 | 新增 `int16 track_force_bias = 0` | ALIGN_FORCE跨帧持久偏差, 解决每帧被basic_line_tracking覆盖导致累积修正为零的问题 |
| 29 | :1005-1027 | ALIGN_FORCE增强: pull_strength 2→6, pull_rows 6→12, 渐变公式修正 `(image_h - i)*strength/rows`, 新增 `track_force_bias` 的EMA累积+限幅±40 | 原2px/6行梯度全部坍塌为1px(整数截断), 单帧修正力不足 |

### 5.2 track_get_error 权重反向修复 (1项)

| # | 行号 | 改动 | 原因 |
|:---:|------|------|------|
| 30 | :1163 | `w = start_row - i + 1` → `w = i - end_row + 1` | 原公式: 底部行(i=59)权重=1, 远处行(i=50)权重=10。注释说"越靠近底部权重越大"但实现完全相反。修复后底部行权重=10 |

### 5.3 track_way.h (1项)

| # | 行号 | 改动 | 原因 |
|:---:|------|------|------|
| 31 | :71 | 新增 `extern int16 track_force_bias` | 供cpu0_main.c读取, 叠加到steer_error上 |

### 5.4 cpu0_main.c (1项)

| # | 行号 | 改动 | 原因 |
|:---:|------|------|------|
| 32 | :688-689 | steer_error叠加track_force_bias | 将ALIGN_FORCE的跨帧累积修正量注入PID, 实现真正的持续回正力 |

### 5.5 清除track_force_bias (2处)

| # | 行号 | 位置 | 触发条件 |
|:---:|------|------|------|
| 33 | :984 | ALIGN_CURVE_EXIT→ALIGN_NONE | 弯道出口正常回正完成 |
| 34 | :1037 | ALIGN_FORCE→ALIGN_NONE | 强制回正完成 |

---

## 六、最终改动统计

| 类别 | 第一轮 | 第二轮 | 合计 |
|------|:---:|:---:|:---:|
| track_way.h | 1 | 1 | 2 |
| track_way.c | 12 | 5 | 17 |
| cpu0_main.c | 13 | 1 | 14 |
| **总计** | **26** | **7** | **33** |

